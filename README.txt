# A Dvorak Type 2 Keylayout

## Installation

* Copy `Dvorak_srs.keylayout` to `~/Library/Keyboard Layouts/`
* Reboot
* Choose layout: `Settings` -> `Keyboard` -> `Other` -> `Dvorak`. Sometimes it states `Not available`, but it is usable anyways.
